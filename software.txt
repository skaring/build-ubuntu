curl 
- sudo apt install curl
Claude Code 
- (curl -fsSL https://claude.ai/install.sh | bash)
- echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc && source ~/.bashrc 
Bitwarden
Obsidian
ksnip (screenshots with annotation)
Ghostty
Vivaldi
YubiKey support (yubikey-manager, fido2-tools, pcscd, scdaemon, Yubico Authenticator)
Herdr
Zed
git
zoxide
fzf
tmux

Corporate (optional, work devices only — see corporate.sh, not part of install.sh):
Microsoft Edge
Microsoft Intune / Company Portal
Microsoft Defender for Endpoint

